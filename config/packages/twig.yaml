twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes:
        - 'bootstrap_=4_horizontal_layout.html.twig'
#        - 'form/fields.html.twig'
    globals:
        version: '%app.version%'
        title: '%app.title%'
        name: '%app.name%'
        description: '%app.description%'
        footer: '%app.footer%'
        author: '%app.author%'