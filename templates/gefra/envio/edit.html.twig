{% extends 'base.html.twig' %}
{% form_theme form 'form/horizontal-form-fields.html.twig' %}
{% block title %}{{ 'gefra.envio.edit-title'|trans({'%name%' : envio.solicitacao }) }}{% endblock %}

{% block menu %}
    {% embed '_menu.html.twig' %}
        {% block app_panel %}
            {% include 'gefra/_app-menu.html.twig' %}
        {% endblock %}
    {% endembed %}
{% endblock menu %}

{% block user_content %}
    <div class="content-wrapper">
        {{ parent() }} {# Printing Messages inside Wrapper Content #}
        <div class="container">

            <!-- Breadcrumbs-->
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('home') }}">{{ 'home'|trans }}</a></li>
                <li class="breadcrumb-item"><a href="{{ path('gefra_envio_index') }}">{{ 'gefra.envio.index'|trans }}</a>
                </li>
                <li class="breadcrumb-item">{{ 'edit'|trans }}</li>
            </ol>
            <div class="row">
                <div class="col-12">
                    <h1>{{ 'gefra.envio.edit-title'|trans({'%name%' : envio.solicitacao }) }}</h1>

                    <div class="card mb-3 border-apps-3">
                        <div class="card-header bg-apps-3 text-white">
                            <i class="fa fa-2x fa-edit fa-2x"></i>
                            {{ 'gefra.envio.edit-form-title'|trans({'%name%' : envio.solicitacao })|raw }}</em></strong>
                        </div>
                        <div class="card-body form-with-cols">
                            {{ include('gefra/envio/_form.html.twig') }}

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
