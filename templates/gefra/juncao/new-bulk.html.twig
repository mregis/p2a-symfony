{% extends 'base.html.twig' %}

{% block title %}{{ 'gefra.juncao.new-bulk-title'|trans }}{% endblock %}

{% block menu %}
    {% embed '_menu.html.twig' %}
        {% block app_panel %}
            {% include 'gefra/_app-menu.html.twig' %}
        {% endblock %}
    {% endembed %}
{% endblock menu %}

{% block user_content %}
    <div class="content-wrapper">
        {{ parent() }} {# Printing Messages inside Wrapper Content #}
        <div class="container">
            <!-- Breadcrumbs-->
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('gefra-home') }}">{{ 'home'|trans }}</a></li>
                <li class="breadcrumb-item"><a href="{{ path('gefra_juncao_index') }}">{{ 'gefra.juncao.index'|trans }}</a></li>
                <li class="breadcrumb-item active">{{ 'new-bulk'|trans }}</li>
            </ol>
            <div class="row">
                <div class="col-12">
                    <h1>{{ 'gefra.juncao.new-bulk-title'|trans }}</h1>

                    {{ form_start(form) }}
                    <div class="card mb-3 border-apps-0">
                        <div class="card-header bg-apps-0 text-white">
                            <div class="row">
                                <div class="col-sm-8">
                                    <i class="fa fa-2x fa-plus-circle fa-2x"></i>
                                    {{ 'gefra.juncao.new-bulk-form-title'|trans }}
                                </div>
                            </div>
                        </div>
                        <div class="card-body form-with-cols">
                            <div class="alert alert-info">
                                <p>{{ 'gefra.juncao.bulk-form-description'|trans|raw }}</p>

                                <p>
                                    <a class="link card-link" href="{{ path('gefra_samplefile', {'source': 'juncao'}) }}">
                                        {{ 'gefra.juncao.bulk-example-link'|trans }}
                                    </a>
                                </p>
                            </div>
                            <fieldset>
                                {{ form_row(form.registry) }}
                            </fieldset>

                        </div>
                        <div class="card-footer">
                            <div class="row form-group justify-content-md-center">
                                <div class="col-4">
                                    <button class="btn btn-primary btn-block " type="submit">
                                        <i class="fa fa-save"></i> {{ button_label|default('save')|trans }}
                                    </button>
                                </div>
                                <div class="col-4">
                                    <a class="btn btn-secondary btn-block" href="{{ path('gefra_juncao_index') }}">
                                        <i class="fa fa-close"></i> {{ button_label|default('cancel')|trans }}
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
