<form method="post" action="{{ path('gefra_operador_delete', {'id': operador.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ operador.id) }}">
    <button class="btn">Delete</button>
</form>