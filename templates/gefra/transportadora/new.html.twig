{% extends 'base.html.twig' %}

{% block title %}{{ 'gefra.transportadora.new'|trans }}{% endblock %}

{% block menu %}
    {% embed '_menu.html.twig' %}
        {% block app_panel %}
            {% include 'gefra/_app-menu.html.twig' %}
        {% endblock %}
    {% endembed %}
{% endblock menu %}

{% block user_content %}
    <div class="content-wrapper">
        {{ parent() }} {# Printing Messages inside Wrapper Content #}
        <div class="container">

            <!-- Breadcrumbs-->
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('gefra-home') }}">{{ 'home'|trans }}</a></li>
                <li class="breadcrumb-item"><a href="{{ path('gefra_transportadora_index') }}">{{ 'gefra.transportadora.index'|trans }}</a>
                </li>
                <li class="breadcrumb-item">{{ 'new'|trans }}</li>
            </ol>
            <div class="row">
                <div class="col-12">
                    <h1>{{ 'gefra.transportadora.new'|trans }}</h1>

                    <div class="card mb-3 border-apps-5">
                        <div class="card-header bg-apps-5">
                            <div class="row">
                                <div class="col-8">
                                    <i class="fa fa-2x fa-plus-circle fa-2x"></i>
                                    {{ 'gefra.transportadora.new-form-title'|trans }}
                                </div>
                            </div>
                        </div>
                        <div class="card-body form-with-cols">
                            {{ include('gefra/transportadora/_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
