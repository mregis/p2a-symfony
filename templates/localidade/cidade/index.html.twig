{% extends 'base.html.twig' %}

{% block title %}Cidade index{% endblock %}

{% block body %}
    <h1>Cidade index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cidade in cidades %}
            <tr>
                <td>{{ cidade.id }}</td>
                <td>{{ cidade.nome }}</td>
                <td>
                    <a href="{{ path('show-cidade', {'id': cidade.id}) }}">show</a>
                    <a href="{{ path('edit-cidade', {'id': cidade.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('new-cidae') }}">Create new</a>
{% endblock %}