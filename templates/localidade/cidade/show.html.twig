{% extends 'base.html.twig' %}

{% block title %}Cidade{% endblock %}

{% block body %}
    <h1>Cidade</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cidade.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ cidade.nome }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('localidade_cidade_index') }}">back to list</a>

    <a href="{{ path('localidade/cidade_edit', {'id': cidade.id}) }}">edit</a>

    {{ include('localidade/cidade/_delete_form.html.twig') }}
{% endblock %}