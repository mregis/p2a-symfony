{% extends 'base.html.twig' %}

{% block title %}Regiao{% endblock %}

{% block body %}
    <h1>Regiao</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ regiao.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ regiao.nome }}</td>
            </tr>
            <tr>
                <th>Sigla</th>
                <td>{{ regiao.sigla }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('localidade_regiao_index') }}">back to list</a>

    <a href="{{ path('localidade_regiao_edit', {'id': regiao.id}) }}">edit</a>

    {{ include('localidade/regiao/_delete_form.html.twig') }}
{% endblock %}