{% extends 'base.html.twig' %}
{% block title %}{{ 'main.transportadora.edit-title'|trans({'%name%' : transportadora.nome }) }}{% endblock %}

{% block menu %}
    {% embed '_menu.html.twig' %}
        {% block app_panel %}
            {% include 'main/_app-menu.html.twig' %}
        {% endblock %}
    {% endembed %}
{% endblock menu %}

{% block user_content %}
    <div class="content-wrapper">
        {{ parent() }} {# Printing Messages inside Wrapper Content #}
        <div class="container">

            <!-- Breadcrumbs-->
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('home') }}">{{ 'home'|trans }}</a></li>
                <li class="breadcrumb-item"><a href="{{ path('main_transportadora_index') }}">{{ 'main.transportadora.index'|trans }}</a>
                </li>
                <li class="breadcrumb-item">{{ 'edit'|trans }}</li>
            </ol>
            <div class="row">
                <div class="col-12">
                    <h1>{{ 'main.transportadora.edit-title'|trans({'%name%' : transportadora.nome }) }}</h1>

                    <div class="card mb-3">
                        <div class="card-header bg-apps-1">
                            <i class="fa fa-2x fa-edit fa-2x"></i>
                            {{ 'main.transportadora.edit-form-title'|trans({'%name%' : transportadora.nome })|raw }}</em></strong>
                        </div>
                        <div class="card-body form-with-cols">
                            {{ include('main/transportadora/_form.html.twig') }}

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
