{% extends 'base.html.twig' %}

{% block title %}Unidade{% endblock %}

{% block body %}
    <h1>Unidade</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unidade.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ unidade.codigo }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ unidade.nome }}</td>
            </tr>
            <tr>
                <th>Cnpj</th>
                <td>{{ unidade.cnpj }}</td>
            </tr>
            <tr>
                <th>Logradouro</th>
                <td>{{ unidade.logradouro }}</td>
            </tr>
            <tr>
                <th>Bairro</th>
                <td>{{ unidade.bairro }}</td>
            </tr>
            <tr>
                <th>Cidade</th>
                <td>{{ unidade.cidade }}</td>
            </tr>
            <tr>
                <th>Uf</th>
                <td>{{ unidade.uf }}</td>
            </tr>
            <tr>
                <th>Cep</th>
                <td>{{ unidade.cep }}</td>
            </tr>
            <tr>
                <th>Ativo</th>
                <td>{{ unidade.ativo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('main_unidade_index') }}">back to list</a>

    <a href="{{ path('main_unidade_edit', {'id': unidade.id}) }}">edit</a>

    {{ include('main_unidade/_delete_form.html.twig') }}
{% endblock %}