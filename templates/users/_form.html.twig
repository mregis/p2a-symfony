{{ form_start(form) }}

{{ form_row(form.name) }}
{{ form_row(form.email) }}
{{ form_row(form.username) }}
{{ form_row(form.profile) }}
{{ form_row(form.isActive) }}

<button class="btn btn-primary col-md-2" type="submit">
    <i class="fa fa-save"></i> {{ button_label|default('save')|trans }}
</button>
<a class="btn btn-secondary col-md-2" href="{{ path('main_user_index') }}">
    <i class="fa fa-close"></i> {{ button_label|default('cancel')|trans }}
</a>
{{ form_end(form) }}